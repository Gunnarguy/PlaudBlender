"""Legacy processing pipeline (transcript chunking + indexing).

These modules exist primarily for backward compatibility with the existing
unit tests and the earlier "segments" pipeline.

Chronos lives under `src/chronos/` and is the forward-looking pipeline.
"""
